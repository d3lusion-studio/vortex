#pragma once
// Kept so that #include "vortex/asset/texture_asset.hpp" still resolves. TextureAsset and
// TextureHandle moved to asset_types.hpp when the manager stopped being texture-only.
#include "vortex/asset/asset_types.hpp"
