---
title: Giới thiệu
description: Vortex là gì, không là gì, và bạn có nên dùng nó không.
---

Vortex là một **game engine C++20 hướng dữ liệu, code-first**. Không có editor, không có định dạng
project độc quyền: game của bạn là một chương trình C++ link tới một thư viện.

## Hiện đã có gì

- **ECS** — registry sparse-set với component lưu đóng gói liên tục. System là hàm thuần.
- **Renderer** — sprite batching, tilemap (import `.tmj` từ Tiled), text, particle, và nhánh 3D với
  PBR GGX, shadow map PCF, HDR bloom + tonemap ACES.
- **RHI** — lớp trừu tượng backend với hai hiện thực: Vulkan và WebGPU (wgpu-native). Không một
  dòng code nào phía trên lớp này nhắc tới tên một graphics API.
- **Physics** — rigid body 2D, collider, joint, raycast và sensor thông qua Box2D.
- **Platform** — cửa sổ và input qua GLFW, cùng một lớp input-map phía trên.

## Vortex *không* phải là gì

- **Không phải engine xoay quanh editor.** Nếu bạn muốn kéo thả prefab vào viewport, hãy dùng Godot
  hoặc Unity. Vortex gần với Bevy hơn: bạn viết scene bằng code.
- **Chưa ổn định.** API còn thay đổi giữa các bản phát hành. Hãy ghim version.
- **Không phải engine 3D-first.** 3D chạy được, nhưng 2D mới là phần hoàn chỉnh.

## Đi tiếp

<Cards>
  <Card title="Bắt đầu" href="/vi/docs/getting-started" description="Build engine và chạy cửa sổ đầu tiên trong mười phút." />
  <Card title="Kiến trúc" href="/vi/docs/architecture" description="Các module ghép với nhau ra sao, và vì sao có lớp RHI." />
</Cards>
